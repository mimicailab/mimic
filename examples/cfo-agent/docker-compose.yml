services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: mimic
      POSTGRES_PASSWORD: mimic
      POSTGRES_DB: mimic_cfo
    ports:
      - "5435:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U mimic -d mimic_cfo"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
