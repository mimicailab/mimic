services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: mimic
      POSTGRES_PASSWORD: mimic
      POSTGRES_DB: mimic_billing
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U mimic -d mimic_billing"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
