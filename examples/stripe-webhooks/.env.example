# Shared secret used to sign/verify the Stripe-Signature header.
# Must match events.stripe.config.secret in mimic.json.
STRIPE_WEBHOOK_SECRET=whsec_demo

# Optional overrides
# PORT=3000                          # webhook-handler.mjs listen port
# MIMIC_BASE_URL=http://localhost:4101  # mock API base URL used by demo.mjs
