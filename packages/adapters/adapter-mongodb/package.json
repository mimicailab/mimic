{
  "name": "@mimicai/adapter-mongodb",
  "version": "0.4.0",
  "description": "MongoDB database adapter for Mimic — insertMany batching, schema inference, auto-indexing",
  "type": "module",
  "license": "Apache-2.0",
  "author": "Mimic AI Lab",
  "homepage": "https://github.com/mimicailab/mimic",
  "repository": {
    "type": "git",
    "url": "https://github.com/mimicailab/mimic.git",
    "directory": "packages/adapters/adapter-mongodb"
  },
  "keywords": [
    "mimic",
    "adapter",
    "mongodb",
    "database",
    "seeding"
  ],
  "engines": {
    "node": ">=22"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@mimicai/core": "workspace:*"
  },
  "peerDependencies": {
    "mongodb": "^6.0.0"
  },
  "peerDependenciesMeta": {
    "mongodb": {
      "optional": true
    }
  },
  "devDependencies": {
    "tsup": "^8.3.6",
    "vitest": "^3.0.5",
    "typescript": "^5.7.3"
  }
}
